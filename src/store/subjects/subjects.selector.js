export const selectSubjects = (state) => state.subjects.subjects;

export const selectTeachers = (state) => state.subjects.teachers;

export const selectIsSubjectsFetching = (state) => state.subjects.isLoading;